services:
  postgres:
    image: leogloriainfnet/postgres-tp2-spring:1.0
    container_name: guild-postgres
    environment:
      POSTGRES_USER: appuser
      POSTGRES_PASSWORD: appuser
      POSTGRES_DB: guilddb
    ports:
      - "5432:5432"
    volumes:
      - guild_pgdata:/var/lib/postgresql/data
      - ./init/:/docker-entrypoint-initdb.d

volumes:
  guild_pgdata:
